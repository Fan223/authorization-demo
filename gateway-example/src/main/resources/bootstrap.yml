spring:
  cloud:
    nacos:
      username: nacos
      password:
      config:
        file-extension: yaml
      server-addr: 127.0.0.1:8848
  application:
    name: gateway
  profiles:
    active: dev